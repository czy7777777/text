#include<stdio.h>
#include<math.h>
int main(void)
{
int a[101]; //下标用到100，所以长度定到101
int i,k,d=2;
for(i=2;i<=100;i++)//开始把每个元素都定为0，相当于做记号
   a[i]=0;
do{                           //do循环是程序的核心，主要任务是把一些100以内的素数都选出来，比如2.3.5的倍数
   k=d;
   if(a[k]==0)//刚开始k=2,这两个语句的目的就是不让a[2]=1,而是从4开始；4的倍数在2的倍数这一轮中已把值改成了1，所以方便跳过4的倍数这一轮，直接进入5的倍数
      k=k+d;
   while(k<=100)//把从开头素数以外的素数的倍数下标的数都变成1，也是做记号
   {
      a[k]=1;
      k=k+d;
    }
    d=d+1;//2的倍数找完找3的倍数，一直往后找
}while(d<=sqrt(100));
for(int j=2;j<=100;j++)//输出素数
  {
     if(a[j]==0)
     printf("%d ",j);
  }
return 0;
}
